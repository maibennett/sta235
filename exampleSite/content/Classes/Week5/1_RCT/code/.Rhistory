)
tbl
tab[5:11,]
tab[4:11,]
names(tab)
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control"),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Competitive", columns = c("V3", "V4","V7", "V8")),
colGroup(name = "Non-competitive", columns = c("V1", "V2","V4","V5")),
colGroup(name = "State 1", columns = c("V1", "V2","V3","V4")),
colGroup(name = "State 2", columns = c("V5", "V6","V7","V8"))
)
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control"),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "State 1", columns = c("V1", "V2","V3","V4")),
colGroup(name = "State 2", columns = c("V5", "V6","V7","V8")),
colGroup(name = "Competitive", columns = c("V3", "V4","V7", "V8")),
colGroup(name = "Non-competitive", columns = c("V1", "V2","V4","V5"))
)
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control"),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Competitive", columns = c("V3", "V4","V7", "V8")),
colGroup(name = "Non-competitive", columns = c("V1", "V2","V4","V5"))
)
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control"),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "State 1", columns = c("V1", "V2","V3","V4")),
colGroup(name = "State 2", columns = c("V5", "V6","V7","V8")),
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8"))
)
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control"),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8"))
)
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control"),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control"),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8"))
),
highlight = TRUE
)
tbl
div(
# this class can be called with CSS now via .salary
class = "tab",
div(
# this can be called with CSS now via .title
class = "title",
h3("Balance Table by Strata")
),
# The actual table
tbl
)
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "2px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8"))
),
highlight = TRUE
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "3px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8"))
),
highlight = TRUE
)
tbl
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "3px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8")),
colGroup(name = "State 1", columns = c("V1","V2","V3", "V4"))
),
highlight = TRUE
)
tbl
setwd("C:/Users/mc72574/Dropbox/Hugo/Sites/sta235/exampleSite/content/Classes/Week4/1_RCT/code")
write.csv(tab, file="balance_gotv.csv")
tab <- read.csv("https://raw.githubusercontent.com/maibennett/sta235/main/exampleSite/content/Classes/Week4/1_RCT/data/voters_covariates.csv")
library(reactable)
tab <- read.csv("https://raw.githubusercontent.com/maibennett/sta235/main/exampleSite/content/Classes/Week4/1_RCT/data/balance_gotv.csv")
library(reactable)
tab
rnames <- tab$X
tab <- tab[,-1]
rownames(tab) <- rnames
library(reactable)
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "3px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8")),
colGroup(name = "State 1", columns = c("V1","V2","V3", "V4"))
),
highlight = TRUE
)
tab <- read.csv("https://raw.githubusercontent.com/maibennett/sta235/main/exampleSite/content/Classes/Week4/1_RCT/data/balance_gotv.csv")
rnames <- tab$X
tab <- tab[,-1]
rownames(tab) <- rnames
library(reactable)
tbl <- tab[4:11,] %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = TRUE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = TRUE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center"),
style = list(fontFamily = "Fira Sans, sans-serif"),
columns = list(
V1 = colDef(name = "Treat"),
V2 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V3 = colDef(name = "Treat"),
V4 = colDef(name = "Control",
style = list(borderRight = "3px solid rgba(0, 0, 0, 0.1)")),
V5 = colDef(name = "Treat"),
V6 = colDef(name = "Control",
style = list(borderRight = "1px solid rgba(0, 0, 0, 0.1)")),
V7 = colDef(name = "Treat"),
V8 = colDef(name = "Control")
),
columnGroups = list(
colGroup(name = "Non-competitive", columns = c("V1", "V2")),
colGroup(name = "Competitive", columns = c("V3", "V4")),
colGroup(name = "Non-competitive", columns = c("V5", "V6")),
colGroup(name = "Competitive", columns = c("V7", "V8")),
colGroup(name = "State 1", columns = c("V1","V2","V3", "V4"))
),
highlight = TRUE
)
div(
# this class can be called with CSS now via .salary
class = "tab",
div(
# this can be called with CSS now via .title
class = "title",
h3("Balance Table by Strata")
),
# The actual table
tbl
)
tab
tbl
summary(estimatr::lm_robust(vote02 ~ treat_real + factor(state)*factor(competiv), dat = d_s1))
summary(estimatr::lm_robust(vote02 ~ treat_real + factor(state) + factor(competiv), dat = d_s1))
var <- factor(state)*factor(competiv)
var <- factor(d_s1$state)*factor(d_s1$competiv)
tab(var)
table(var)
var <- d_s1$state*d_s1$competiv
table(var)
d_s1 %>% interaction(state, competiv)
d_s1$strata <- interaction(d_s1$state, d_s1$competiv)
head(d_s1$strata)
summary(estimatr::lm_robust(vote02 ~ treat_real + strata, dat = d_s1))
table(d_s1$treat_real, d_s1$contact)
options(htmltools.dir.version = FALSE)
knitr::opts_chunk$set(fig.showtext = TRUE)
tab <- cbind(c("E-mail","","Mail",""),c("19%","(290/1500)","16%","(88/550)"))
colnames(tab) <- c("Treatment","Total")
tbl <- tab %>%
reactable(
# ALL one page (no scrolling or page swapping)
pagination = TRUE,
rownames = FALSE,
# compact for an overall smaller table width wise
compact = FALSE,
# borderless - TRUE or FALSE
borderless = FALSE,
# Stripes - TRUE or FALSE
striped = TRUE,
# fullWidth - either fit to width or not
fullWidth = FALSE,
# apply defaults
# 100 px and align to center of column
defaultColDef = colDef(
align = "center",
minWidth = 200
),
style = list(fontFamily = "Fira Sans, sans-serif"))
div(
# this class can be called with CSS now via .salary
class = "tab",
div(
# this can be called with CSS now via .title
class = "title",
h3("% of New Registrations by Type of Campaign")
),
# The actual table
tbl
)
